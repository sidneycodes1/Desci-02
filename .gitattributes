# Normalize line endings: LF in the repo, native on checkout.
* text=auto

# Foundry / shell scripts must always use LF, even on Windows checkouts.
*.sol text eol=lf
*.s.sol text eol=lf
*.sh text eol=lf
